## Workspace root (no buildable project here)

[dependency-groups]
dev = [
    "microsoft-python-type-stubs",
    "mypy>=1.18.2",
    "pyright>=1.1.407",
]

[tool.mypy]
mypy_path = ["stubs"]

[tool.uv.sources]
microsoft-python-type-stubs = { git = "https://github.com/microsoft/python-type-stubs.git" }

[tool.uv.workspace]
members = ["packages/*"]
